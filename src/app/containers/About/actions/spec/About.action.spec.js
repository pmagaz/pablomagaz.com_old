import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions About', () => {
    it('Should create action getAbout', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.ABOUT_REQUEST),
        request: api.fetchAbout(params)
      };

      expect(Actions.getAbout(params)).toEqual(expectedAction);
*/
    });

  });
});
