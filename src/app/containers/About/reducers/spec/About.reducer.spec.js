import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('About reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.ABOUT_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of About request fail', () => {

      const action = {
        type: types.ABOUT_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
