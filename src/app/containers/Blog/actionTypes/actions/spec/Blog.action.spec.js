import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions Blog', () => {
    it('Should create action getBlog', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.BLOG_REQUEST),
        request: api.fetchBlog(params)
      };

      expect(Actions.getBlog(params)).toEqual(expectedAction);
*/
    });

  });
});
