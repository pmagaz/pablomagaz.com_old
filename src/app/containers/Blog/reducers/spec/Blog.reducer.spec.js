import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('Blog reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.BLOG_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of Blog request fail', () => {

      const action = {
        type: types.BLOG_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
