import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions Contact', () => {
    it('Should create action getContact', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.CONTACT_REQUEST),
        request: api.fetchContact(params)
      };

      expect(Actions.getContact(params)).toEqual(expectedAction);
*/
    });

  });
});
