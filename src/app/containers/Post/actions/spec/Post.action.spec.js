import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions Post', () => {
    it('Should create action getPost', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.POST_REQUEST),
        request: api.fetchPost(params)
      };

      expect(Actions.getPost(params)).toEqual(expectedAction);
*/
    });

  });
});
