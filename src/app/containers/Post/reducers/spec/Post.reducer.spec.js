import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('Post reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.POST_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of Post request fail', () => {

      const action = {
        type: types.POST_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
